schema: src/**/*.graphql
generates:
  ./src/graphql/resolvers-types.ts:
    plugins:
      - typescript-operations
      - typescript-resolvers
    config:
      mappers:
        User: .prisma/client#User as UserModel
        Guide: .prisma/client#Guide as GuideModel

